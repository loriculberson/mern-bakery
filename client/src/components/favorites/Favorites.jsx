
export default function Favorites(){
    /*
    fetch data from single user
    use params
    */
    // const favoritesList = items?.map(item => {
    //     return <li key={item._id}>{item.name}</li>
    // })
    return (
        <div>
            <h2>Favorites List</h2>
            {/* <ul>{favoritesList}</ul> */}
        </div>
    )
}